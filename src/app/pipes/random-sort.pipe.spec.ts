import { RandomSortPipe } from './random-sort.pipe';

describe('RandomSortPipe', () => {
  it('create an instance', () => {
    const pipe = new RandomSortPipe();
    expect(pipe).toBeTruthy();
  });
});
